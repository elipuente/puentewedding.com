import "../styles/globals.css";
import type { AppProps } from "next/app";

const PuenteWedding = ({ Component, pageProps }: AppProps) => (
  <Component {...pageProps} />
);

export default PuenteWedding;
